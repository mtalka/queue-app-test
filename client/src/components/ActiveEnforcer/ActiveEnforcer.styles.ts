import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
    signInDesc: {
        marginTop: 30,
        fontSize: 14,
        fontWeight: 400,
        color: "grey",
    },
    logInLink: {
        fontWeight: 600,
    }
}),
);