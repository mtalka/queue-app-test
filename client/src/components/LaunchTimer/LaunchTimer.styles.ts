import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
    timeLeft: {
        marginTop: 30,
        fontSize: 14,
        fontWeight: 400,
        color:"grey",
    },
    launchTimer: {
        fontSize: 48,
        fontWeight: 600,
    }
}),
);